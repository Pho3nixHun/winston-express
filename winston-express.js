"use strict";
const fs = require('fs');
const path = require('path');
const winston = require('winston');

module.exports = exports = (function(fs, path, winston){
    const simplifiers = require('./assets/')

    return function(req, res, next){

    }
})(fs, path, winston);